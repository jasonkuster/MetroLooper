<phone:PhoneApplicationPage
    x:Class="MetroLooper.LoopPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Landscape" Orientation="Landscape"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">
	<phone:PhoneApplicationPage.Resources>
		<DataTemplate x:Key="LoopItemTemplate">
			<Grid Width="647">
				<TextBlock x:Name="TrackName" TextWrapping="Wrap" Text="{Binding trackName}" VerticalAlignment="Center" FontSize="26.667" Margin="0,2"/>
				<Border x:Name="delBorder" BorderBrush="White" BorderThickness="1" HorizontalAlignment="Right" Height="36" VerticalAlignment="Top" Width="100" Margin="0,2" Visibility="Collapsed">
					<TextBlock x:Name="delBlock" TextWrapping="Wrap" Text="Delete" HorizontalAlignment="Center" VerticalAlignment="Center"/>
				</Border>
			</Grid>
		</DataTemplate>
	</phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent" DataContext="{Binding Source={StaticResource LoopSamples}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="METROLOOPER" Style="{StaticResource PhoneTextNormalStyle}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
        	<StackPanel>
        		<TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="Loop List" VerticalAlignment="Top" Margin="{StaticResource PhoneMargin}"/>
        		<phone:LongListSelector x:Name="loopList" ItemsSource="{Binding SelectedBank.tracks}" HorizontalAlignment="Left" Height="213" VerticalAlignment="Top" SelectionChanged="LongListSelector_SelectionChanged" Width="680" Margin="12,0" ItemTemplate="{StaticResource LoopItemTemplate}"/>
        		<StackPanel Height="100" Orientation="Horizontal" Margin="0">
        			<Button x:Name="continueButton" IsEnabled="{Binding recButtons}" Content="Cont" Click="continueButton_Click" Margin="{StaticResource PhoneMargin}"/>
        			<Button x:Name="stopButton" IsEnabled="{Binding stop}" Content="Stop" Click="stopButton_Click" Margin="{StaticResource PhoneMargin}"/>
        			<Button x:Name="recOneButton" IsEnabled="{Binding recButtons}" Content="One" Click="recOneButton_Click" Margin="{StaticResource PhoneMargin}"/>
        			<Button x:Name="metronomeButton" Content="Metronome" Click="metronomeButton_Click" Margin="{StaticResource PhoneMargin}"/>
        			<Button x:Name="finalizeButton" IsEnabled="{Binding recButtons}" Content="Finalize" Click="finalizeButton_Click" Margin="{StaticResource PhoneMargin}"/>
        		</StackPanel>
        		<ProgressBar x:Name="measureBar" Height="10"/>
        	</StackPanel>

        </Grid>
    </Grid>

</phone:PhoneApplicationPage>